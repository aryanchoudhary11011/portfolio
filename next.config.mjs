/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "bishnoi11011.s3.ap-south-1.amazonaws.com",
      "s3.ap-south-1.amazonaws.com",
    ],
  },
};

export default nextConfig;
